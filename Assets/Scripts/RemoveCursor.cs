using UnityEngine;

// This script is currently not used, as the functionality was moved to DescriptiveTextScript.

public class RemoveCursor : MonoBehaviour {
    void Start() {
        Cursor.visible = false;
    }
}